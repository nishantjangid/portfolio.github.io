/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'out',
    images: {
      unoptimized: true,
    },
    basePath: '/portfolio.github.io',    
};

export default nextConfig;
